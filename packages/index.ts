export const OutReAgent = ({strategy, riskManagement, tradeFrequency}: {strategy: string, riskManagement: boolean, tradeFrequency: string}) => {
  return {
    strategy,
    riskManagement,
    tradeFrequency
  }
}
